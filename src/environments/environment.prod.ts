export const environment = {
	production: true,
	// Same-origin with the shell, which forwards this to the API: keeps the refresh cookie HttpOnly/Secure/SameSite=Strict.
	apiBaseUrl: '/api',
	sourceCodeUrl: 'https://github.com/Premoved/PremovedPrep-App',
	contactEmail: 'contact@premoved.com',
};
